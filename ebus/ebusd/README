ebus Daemon (ebusd) - README
============================

ebus (energy bus) is a serial communication protocoll used in heating systems. 

ebusd connects via a ebus-usb adapter with the ebus. Once it is connected,
it is able to send and receive message with other bus devices. ebusd will be
listen on TCP port 8888 for incoming telnet connections.

The following commands can be received and processed via telnet:
 * get...
 * set...
 * quit
 * shutdown

To build run

$ ./autogen.sh

or this commands in this order

$ aclocal
$ autoconf
$ autoheader
$ automake
$ ./configure
$ make


The author can be contacted at roland.jax@liwest.at.
